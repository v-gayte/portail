---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div
  class={`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 auto-rows-auto w-full ${className}`}
>
  <slot />
</div>
